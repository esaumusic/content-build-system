//const myArbitraryName = require('my-package')

var config = require('./config')()
var { selectedBrand, selectedJob } = config

module.exports = function(gulp, plugins) {
  return function () {
    var stream =
    // -------------------------------------------- Start Task
    // code here! 
    // ---------------------------------------------- End Task
    return stream
  }
}
